import type { Request, Response, NextFunction } from "express";
export default function handleValidatorResult(
    req: Request,
    res: Response,
    next: NextFunction,
) {
    // const result=req.
    next();
}
